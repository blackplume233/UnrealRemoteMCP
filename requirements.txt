# Python >= 3.11
#
# Notes:
# - Generated from the repo-bundled Python environment at `Content/Python/Lib/site-packages`.
# - Intended to reproduce/supplement RemoteMCP's Python-side dependencies (MCP SDK + ASGI/HTTP stack).
#
# Core (MCP Server)
mcp==1.25.0
uvicorn==0.40.0
anyio==4.12.0

# MCP/HTTP stack (pinned to the bundled versions)
starlette==0.50.0
sse-starlette==3.1.2
httpx==0.28.1
httpcore==1.0.9
httpx-sse==0.4.3
h11==0.16.0
python-multipart==0.0.21

# Config / serialization
pydantic==2.12.5
pydantic-settings==2.12.0
pydantic_core==2.41.5
typing_extensions==4.15.0
typing-inspection==0.4.2
annotated-types==0.7.0

# JSON Schema (required by MCP)
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==0.30.0

# Auth / Crypto (MCP depends on pyjwt[crypto])
PyJWT==2.10.1
cryptography==46.0.3
cffi==2.0.0
pycparser==2.23

# CLI / output (optional, pinned to bundled versions)
rich==14.2.0
typer==0.21.0
shellingham==1.5.4
markdown-it-py==4.0.0
mdurl==0.1.2
pygments==2.19.2

# Base deps
click==8.3.1
colorama==0.4.6
attrs==25.4.0
certifi==2026.1.4
idna==3.11
python-dotenv==1.2.1

# Windows deps (mcp pulls pywin32 on win32; bundled version is 311)
pywin32==311

